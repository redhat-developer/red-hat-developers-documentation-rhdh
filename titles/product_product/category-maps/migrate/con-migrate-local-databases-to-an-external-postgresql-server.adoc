:_mod-docs-content-type: CONCEPT

= Migrate local databases to an external PostgreSQL server

[role="_abstract"]
Transfer data from the default local PostgreSQL instance to your external PostgreSQL server by using the database copy script to extract, transfer, and restore catalog data, user sessions, and plugin state.
