:_mod-docs-content-type: REFERENCE

[id="model-to-entity-mapping_{context}"]
= Model-to-entity mapping

[role="_abstract"]
The following table maps the KServe and Kubeflow Model Catalog sources that the connector reads to {backstage} entity kinds.

|===
|Source artifact |{product-very-short}/{backstage} entity kind |{product-very-short}/{backstage} entity type |Purpose

|Model server (KServe `InferenceService`)
|`AiModelServerAPI`
|`ai-model-server`
|Represents a running, accessible AI model server endpoint that is discovered from a KServe `InferenceService` resource.

|Model (from a KServe `InferenceService`)
|`AiModelServerAPI` (`spec.models`)
|`ai-model-server`
|Represents an individual model that is served by the model server. Models are listed in the `spec.models` field of the generated `AiModelServerAPI` entity.

|Model card (Kubeflow Model Catalog)
|TechDocs
|N/A
|Associates a model card from the Kubeflow Model Catalog with the generated entity as {product-very-short} TechDocs. Model cards are imported when the `InferenceService` is annotated to reference a catalog source and model.
|===

After you install and configure the {kserve-kubeflow-connector-name-short}, the transfer of information from your clusters commences automatically on the configured schedule.
