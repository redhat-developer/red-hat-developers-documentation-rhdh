. To disable the guest login option, in the {my-app-config-file} file, remove or comment out the entire `auth.provider.guest` section.
+
[source,yaml]
----
auth:
  provider:
#    guest:
#      dangerouslyAllowOutsideDevelopment: true
----
