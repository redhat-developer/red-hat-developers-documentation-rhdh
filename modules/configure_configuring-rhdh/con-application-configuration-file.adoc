:_mod-docs-content-type: CONCEPT

[id="application-configuration-file_{context}"]
= Application configuration file

[role="_abstract"]
The `{my-app-config-file}` file is the main {product-short} configuration file. It controls application behavior including URLs, authentication, catalog sources, database connections, and plugin settings.

The following are key sections in `{my-app-config-file}`:

`app`:: Frontend settings such as the application title and base URL.

`backend`:: Backend settings including the base URL, CORS origin, database connection, authentication, and cache.

`auth`:: Authentication provider configuration.

`catalog`:: Software Catalog locations and entity providers.

`techdocs`:: TechDocs builder and storage configuration.

`proxy`:: Proxy endpoints for external services.

`permission`:: RBAC permission framework configuration.

The `{my-app-config-file}` file supports `$\{VAR_NAME}` syntax that resolves at runtime from environment variables. You can inject environment variables through Kubernetes secrets or config maps, which allows you to keep sensitive values such as credentials and tokens out of your configuration files.

You can provision the `{my-app-config-file}` file by using either the Operator or the Helm chart:

* With the Operator: on OCP, the Operator may automatically set `app.baseUrl`, `backend.baseUrl`, and `backend.cors.origin` from the route in the default application configuration. Create a config map containing your `{my-app-config-file}` and reference it in the `{product-custom-resource-type}` custom resource.
* With the Helm chart: set the base URL fields manually. You can inline your configuration in the Helm chart values at `upstream.backstage.appConfig`, or reference external config maps through `upstream.backstage.extraAppConfig`.

[role="_additional-resources"]
.Additional resources

* xref:provision-your-custom-rhdh-configuration_provision-and-use-your-custom-rhdh-configuration[Provisioning your custom {product} configuration]
* {helm-chart-configuration-reference-book-url}[{helm-chart-configuration-reference-book-title}]
