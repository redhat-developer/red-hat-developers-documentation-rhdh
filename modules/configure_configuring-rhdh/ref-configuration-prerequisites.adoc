:_mod-docs-content-type: REFERENCE
[id="configuration-prerequisites_{context}"]
= Configuration prerequisites

[role="_abstract"]
To enable schema-based isolation, configure the backend.database settings in the `app-config.yaml` file to use the PostgreSQL client. This configuration ensures the plugin divider functions correctly within your infrastructure.
