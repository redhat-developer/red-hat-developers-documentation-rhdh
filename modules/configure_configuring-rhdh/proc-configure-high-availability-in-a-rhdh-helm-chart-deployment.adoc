:_mod-docs-content-type: PROCEDURE

[id="configure-high-availability-in-a-rhdh-helm-chart-deployment_{context}"]
= Configure with the Helm chart

[role="_abstract"]
Configure high availability for Helm deployments by setting the `replicaCount` value to greater than 1 in the Helm configuration file.

.Procedure
* In your Helm chart configuration file, set `replicaCount` to a value greater than `1`.
+
For example, to configure two replicas (one backup instance):
+
[source,yaml,subs="+attributes,+quotes"]
----
replicaCount: 2
----
