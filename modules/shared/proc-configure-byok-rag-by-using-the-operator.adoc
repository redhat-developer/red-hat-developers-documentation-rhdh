:_mod-docs-content-type: PROCEDURE

[id="configure-byok-rag-by-using-the-operator_{context}"]
= Configure BYOK RAG by using the Operator

[role="_abstract"]
Deploy your BYOK container image as an init container and register the custom knowledge source in `lightspeed-stack.yaml` in your Operator-based {product} ({product-very-short}) deployment.

.Prerequisites
* You have packaged your vector store as a container image as described in xref:package-a-byok-rag-container-image_{context}[Package a BYOK RAG container image].
* You have the generated `vector_db_id` value from xref:generate-a-vector-database-for-byok-rag_{context}[Generate a vector database for BYOK RAG].
* The {product-very-short} Operator is installed on your cluster.
* You have cluster administrator privileges.
* The BYOK container image is accessible from your cluster.

.Procedure

. Prepare a complete `lightspeed-stack.yaml` file with the BYOK sections added, then create a `ConfigMap` from it:
+
[WARNING]
====
The mounted file replaces the product-provided `lightspeed-stack.yaml` in its entirety; it is not merged. If you create the `ConfigMap` from only the BYOK sections, you remove other supported or required settings, including the service, authentication, cache, user-data and profile, MCP, and other product configuration, and {ia-short} can fail to start or lose required functionality. Always create the `ConfigMap` from the complete configuration for your {product-very-short} version with the BYOK sections added to it.
====
+
.. Copy or export the complete product-provided `lightspeed-stack.yaml` for your installed {product-very-short} version to a local file.
.. Ensure that the `sentence_transformers` embedding provider is present under `inference.providers`, then register the store under `rag.byok.stores` and activate it under `rag.retrieval`. Add the following fragment to the local file, merging it with the existing sections rather than replacing them:
+
[source,yaml,subs="+attributes,+quotes"]
----
inference:
  providers:
    - type: sentence_transformers  # Add only if not already present
rag:
  byok:
    stores:
      - rag_id: custom-docs  <1>
        backend: faiss
        embedding_model: sentence-transformers/all-mpnet-base-v2
        embedding_dimension: 768
        vector_db_id: vs_replace_with_generated_id  <2>
        db_path: /rag-content-byok/vector_db/custom_docs/faiss_store.db  <3>
        score_multiplier: 1.0  # Used by Inline RAG only
  retrieval:
    tool:
      sources:
        - custom-docs  <4>
----
+
<1>:: The `rag_id` identifies the retrieval source. Use the same value under `rag.retrieval.tool.sources`.
<2> Replace with the `vector_db_id` value that you read from the generated `llama-stack.yaml` file. The runtime configuration must use the exact same ID.
<3> Must match the path where the init container copies the database inside the {ia-short} container. This procedure uses the dedicated `/rag-content-byok` mount path so that the BYOK content does not replace or shadow any existing product-provided RAG content in `/rag-content`.
<4> Must match `rag_id`. If you have other sources, such as product documentation, list each required source under `sources`.
+
.. Create the `ConfigMap` from the completed local file:
+
[source,terminal]
----
$ oc create configmap lightspeed-stack-byok \
    --from-file=lightspeed-stack.yaml \
    --dry-run=client -o yaml | oc apply -f -
----

. Update your `{backstage}` custom resource (CR) to mount the custom `ConfigMap`, and use `spec.deployment.patch` to add the BYOK init container, shared volume, and volume mount. The `spec.deployment.patch` value is a deployment fragment that the Operator merges with the generated deployment, so the BYOK init container is added alongside any existing init containers, and the dedicated `/rag-content-byok` mount does not replace or shadow any existing product-provided RAG content:
+
[source,yaml,subs="+attributes,+quotes"]
----
apiVersion: rhdh.redhat.com/v1alpha5
kind: {product-custom-resource-type}
metadata:
  name: lightspeed-rhdh
spec:
  application:
    extraFiles:
      configMaps:
        - name: "lightspeed-stack-byok"  <1>
          mountPath: /app-root
          key: lightspeed-stack.yaml
          containers:
            - lightspeed-core
  deployment:
    patch:
      spec:
        template:
          spec:
            volumes:
              - name: byok-rag
                emptyDir: {}
            initContainers:
              - name: byok-rag-init  <2>
                image: quay.io/your-organization/rhdh-byok:1.0.0
                imagePullPolicy: IfNotPresent
                command: ["/bin/sh", "-c"]
                args:
                  - |
                    set -eu
                    mkdir -p /rag-content-byok/vector_db
                    cp -R /byok/vector_db/. /rag-content-byok/vector_db/
                volumeMounts:
                  - name: byok-rag
                    mountPath: /rag-content-byok
                securityContext:
                  allowPrivilegeEscalation: false
                  readOnlyRootFilesystem: true
                  runAsNonRoot: true
                  capabilities:
                    drop: ["ALL"]
                  seccompProfile:
                    type: RuntimeDefault
            containers:
              - name: lightspeed-core
                volumeMounts:
                  - name: byok-rag
                    mountPath: /rag-content-byok
----
+
<1>:: References the `ConfigMap` created in the previous step. This replaces the default `lightspeed-stack.yaml` for the `lightspeed-core` container.
<2> The init container runs before {ia-short} starts and copies the vector database to the shared `byok-rag` volume. The dedicated `/rag-content-byok` mount path does not replace or shadow any existing product-provided RAG content.

. Apply the updated `{backstage}` CR:
+
[source,terminal]
----
$ oc apply -f <backstage_cr_file>.yaml
----

.Verification
* xref:verify-byok-rag-retrieval_{context}[Verify the deployed vector store]

.Additional resources
* xref:ref-byok-rag-configuration-fields-for-lightspeed-stack_{context}[BYOK RAG configuration fields for `lightspeed-stack.yaml`]
