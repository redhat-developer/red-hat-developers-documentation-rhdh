:_mod-docs-content-type: PROCEDURE

[id="enable-data-persistence-for-developer-lightspeed-notebooks_{context}"]
= Persist Notebook data to retain sessions across restarts

[role="_abstract"]
To keep Notebook sessions, documents, and AI history after a pod restart, back the {ls-short} runtime volume with persistent storage instead of the default ephemeral volume.

By default, {lcs-name} ({lcs-short}) writes Notebook vector data, the conversation cache, and feedback to paths under the `/tmp` directory on an ephemeral runtime volume, which the system clears during a pod restart. In {product-very-short} 2.1, you keep these paths in the unified `lightspeed-stack.yaml` file and make the data persistent through your deployment method. You do not edit a separate `config.yaml` file or any Llama Stack storage backend.

.Prerequisites

* By using the link:https://docs.redhat.com/en/documentation/openshift_container_platform/{ocp-version}/html-single/cli_tools/index#cli-about-cli_cli-developer-commands[{openshift-cli}], you have access, with developer permissions, to the {ocp-short} cluster that has your {product-short} instance.
* You have authored and provisioned a custom config map for your deployment. For more information, see link:{configuring-book-link}#provision-your-custom-rhdh-configuration_provision-and-use-your-custom-rhdh-configuration[Provision your custom {product} configuration].
* A Persistent Volume Claim (PVC) is provisioned in your cluster.

.Procedure

. In your unified `lightspeed-stack.yaml` file, keep the shipped runtime data paths for the Notebook vector store, conversation cache, and feedback storage:
+
[source,yaml]
----
vector_store:
  default_provider: notebooks
  providers:
    - id: notebooks
      type: faiss
      embedding_model: nomic-ai/nomic-embed-text-v1.5
      embedding_dimension: 768
      config:
        path: /tmp/vector_db/notebooks/faiss_store.db
conversation_cache:
  type: sqlite
  sqlite:
    db_path: /tmp/cache.db
user_data_collection:
  feedback_enabled: true
  feedback_storage: /tmp/data/feedback
----

. Back the {lcs-short} runtime volume with your PVC through your deployment method, so that the runtime data paths survive pod replacement. For a Helm deployment, configure the runtime volume to use your `persistentVolumeClaim` instead of the default ephemeral volume. For an Operator deployment, provision persistent storage for the runtime data paths according to the Operator storage guidance.
+
[NOTE]
====
The default ephemeral runtime volume does not survive pod replacement. Confirm the exact runtime volume settings for your deployment method in the {product-very-short} installation documentation before you rely on persistence.
====

. Apply the updated configuration, and then restart the {lcs-short} service.

.Verification

. In {product}, create a Notebook and upload a test document.
. Send a message to the virtual assistant, and verify that the response is based on the document.
. Restart the pod:
+
[source,terminal,subs="+quotes"]
----
$ oc delete pod _<pod_name>_
----
. After the pod recovers, refresh the *My Notebooks* dashboard.
. Verify that the Notebook and the uploaded file are still accessible.
