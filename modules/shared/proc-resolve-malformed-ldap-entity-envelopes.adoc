:_mod-docs-content-type: PROCEDURE

[id="resolve-malformed-ldap-entity-envelopes_{context}"]
= Resolve malformed LDAP entity envelopes

[role="_abstract"]
This error occurs when a user being ingested from LDAP has no value for the `name` field, which is mapped to the `uid` LDAP attribute by default.

.Prerequisites
* You have configured LDAP user provisioning in {product-short}.

.Procedure
* Add a filter to the LDAP users configuration to exclude users without a `uid`:
+
[source,yaml]
----
catalog:
  providers:
    ldapOrg:
      default:
        users:
          - dn: OU=Users,DC=example,DC=com
            options:
              filter: (uid=*)
----

.Verification
* Check the backend logs for successful LDAP synchronization without the malformed entity envelope error:
+
----
LdapOrgEntityProvider:default refresh succeeded
----

.Additional resources
* For more information about LDAP user filters, see xref:enable-user-provisioning-with-ldap_provision-users-with-ldap[Enable user provisioning with LDAP].
