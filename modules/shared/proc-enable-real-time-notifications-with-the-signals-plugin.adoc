:_mod-docs-content-type: PROCEDURE

[id="enable-real-time-notifications-with-the-signals-plugin_{context}"]
= Enable real-time notifications with the signals plugin

[role="_abstract"]
Enable the signals plugin so that your team receives notifications instantly in {product} ({product-very-short}) instead of waiting for the next polling interval.

Without the signals plugin, the notifications plugin checks for new events periodically, which can delay delivery by several seconds. Enabling signals eliminates that delay so your team can act on workflow events, template updates, and other changes as they happen.

.Prerequisites

* You have {configuring-book-link}[added a custom {product-short} application configuration].
* You have installed and enabled the {product-very-short} notification plugins:
** `backstage-plugin-notifications`
** `backstage-plugin-notifications-backend-dynamic`

.Procedure

. Open your {product-very-short} dynamic plugin configuration file.
. Add the signals frontend and backend plugins:
+
[source,yaml]
----
plugins:
  - package: ./dynamic-plugins/dist/backstage-plugin-signals
    disabled: false
  - package: ./dynamic-plugins/dist/backstage-plugin-signals-backend-dynamic
    disabled: false
----
+
. Save the configuration file and restart your {product-very-short} instance.

.Verification

. Log in to your {product} instance.
. Trigger a notification event, such as updating a Software Template version.
. Verify that the notification appears in the {product-very-short} header without requiring a page refresh.
