:_mod-docs-content-type: PROCEDURE

[id="disable-by-using-the-operator_{context}"]
= Disable {ls-short} by using the Operator

[role="_abstract"]
Disable the {ls-short} chat interface and stop associated container processes to remove the service from your Operator-backed deployment.

.Prerequisites
* You have access to the cluster where your instance is deployed.
* You have cluster administrator privileges.

.Procedure

. Open your {backstage} custom resource (CR) YAML file.
. In the `spec` section, set the `enabled` flag to `false` for the `intelligent-assistant` flavour. This disables the chat interface and prevents the Operator from injecting unconfigured sidecar containers:
+
// TODO (RHIDP-16107): Confirm the CR flavour name. rhdh-operator PR 3370 renamed the flavour directory
// to "intelligent-assistant" and gates on IsFlavourEnabled(spec, "intelligent-assistant"), but some PR
// comments still say "lightspeed". Verify the exact spec.flavours[].name value once the PR merges.
[source,yaml,subs="+attributes,+quotes"]
----
apiVersion: rhdh.redhat.com/v1alpha5
kind: {product-custom-resource-type}
metadata:
  name: lightspeed-disabled
spec:
  flavours:
    - name: intelligent-assistant
      enabled: false
----
. Apply the updated custom resource manifest to your cluster:
+
[source,bash]
----
oc apply -f <backstage_cr_file>.yaml
----

.Verification
. Log in to your console instance.
. Verify that the *Intelligent Assistant* button no longer appears on the home page.
