:_mod-docs-content-type: CONCEPT

[id="prepare-your-development-environment_{context}"]
= Prepare your development environment

[role="_abstract"]
Before creating or converting plugins for {product} ({product-very-short}), set up a local development toolchain to write, convert, and package plugins for deployment.

Required skills and languages:
To develop dynamic plugins, you must have experience with the following:

JavaScript and TypeScript:: Used for {backstage} frontend and backend development.
React:: Used for building frontend plugin components.
Node.js ecosystem:: Includes package management (NPM/Yarn) and module handling.

== The development toolchain
The following tools are required to initialize, build, and package your plugins:

Node.js (via NVM):: Node.js is the engine that runs JavaScript on your computer.
+
{product-very-short} requires Node v22. Use Node Version Manager (NVM) to switch between Node versions and ensure compatibility with the {product-very-short} backend system.

Yarn 4:: Yarn is a package manager that handles all the library dependencies that your application needs.
+
The {backstage} project structure is optimized for Yarn (specifically Yarn Classic 1.x) to manage workspaces and dependencies efficiently.

Containerization tools (Docker or Podman):: These tools used to run containers and package applications.
+
* Packaging: Dynamic plugins are distributed as OCI images. Use Docker or Podman to package your derived plugin assets into an image that can be pushed to a registry, for example, Quay.io and sideloaded into {product-very-short}.

{product-very-short} plugin tools::
These specialized tools convert standard plugins into the dynamic architecture required by {product-very-short}.
+
* {product-very-short} Plugin Factory and {product-cli}: These tools assist converting existing standard {backstage} plugins into the {product-very-short} dynamic plugin format.
+
* {product-cli} (`@red-hat-developer-hub/cli`): This command-line tool is critical for the export process. It allows you to run commands like export-dynamic-plugin, which repackages your code into a derived package containing the necessary configuration (like Scalprum for frontend) and dependency handling (bundling private dependencies versus sharing platform dependencies)

.Additional resources
* link:https://github.com/redhat-developer/rhdh-dynamic-plugin-factory[{product-very-short} Plugin Factory]
* link:https://github.com/redhat-developer/rhdh-cli[{product-cli}]

//  Overview of content required: This module will detail the technical prerequisites for dynamic plugin development, including required languages, skills, and tools.

//  ===========


// Understand the technical prerequisites for dynamic plugin development, including required languages, skills, and tools such as Node.js, NPM, Yarn, {product-very-short} Plugin Factory, and {product-cli}.

// =============

// 1. Install Node.js (via NVM)
// What it is: Node.js is the engine that runs JavaScript on your computer.
// Why NVM: Node Version Manager (NVM) lets you switch between versions easily, as {backstage} requires specific LTS (Long Term Support) versions (v18 or v20).
// How to install:
// macOS/Linux: curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash
// Windows: Download nvm-windows.
// Command: nvm install 20 && nvm use 20
// 2. Install Yarn (Classic)
// What it is: A package manager that handles all the libraries (dependencies) your app needs. {backstage} is optimized for Yarn.
// How to install: npm install --global yarn
// 3. Install Docker
// What it is: Used by {backstage} to run "containers" for tech documentation (TechDocs) or to package your app for production.
// How to install: Download Docker Desktop.

// ================
