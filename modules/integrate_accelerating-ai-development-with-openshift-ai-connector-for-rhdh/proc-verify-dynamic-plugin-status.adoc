:_mod-docs-content-type: PROCEDURE

[id="verify-dynamic-plugin-status_{context}"]
= Verify dynamic plugin status

[role="_abstract"]
Validate that the dynamic plugins have been successfully installed into your {product-very-short} project Pod.

.Procedure

. Use the following command to check the installation logs:
+
[source,bash,subs=+attributes]
----
$ oc logs -c install-dynamic-plugins deployment/<your {product-very-short} deployment>
----

. In the `install-dynamic-plugin` logs, verify successful installation of the following plugins:
+
** `red-hat-developer-hub-backstage-plugin-catalog-backend-module-model-catalog` (Entity Provider)
** `red-hat-developer-hub-backstage-plugin-catalog-techdoc-url-reader-backend` (TechDoc URL Reader)
