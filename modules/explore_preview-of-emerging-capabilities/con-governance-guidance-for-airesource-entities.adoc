:_mod-docs-content-type: CONCEPT

[id="governance-guidance-for-airesource-entities_{context}"]
= Governance guidance for AiResource entities

[role="_abstract"]
As a Platform Engineer, you can use `AiResource` entities to publish governed, authoritative AI content assets for your organization. Use the following guidance to manage the ownership, lifecycle, and availability of AI content assets.

== Ownership and access control
Each `AiResource` entity declares an owner in the `spec.owner` field. Ownership and access are enforced by the existing catalog role-based access control (RBAC) model. No RBAC controls are specific to the `AiResource` kind; the permissions that govern other catalog entities apply to `AiResource` entities in the same way.

== Lifecycle
Because the `AiResource` kind is experimental, set `lifecycle: experimental` until the schema stabilizes. This value signals to developers that the asset definition might change.

== Air-gapped environments
`AiResource` entities are fully supported in air-gapped environments:

* The `catalog-info.yaml` file is ingested from your internal source control.
* OCI image references can point to internal registries, such as an on-premise Quay.io instance or an internal container registry.

Because the catalog records the asset location only and does not pull or run OCI image content, external connectivity is not required for catalog indexing.

== Promoting best practices
To encourage adoption, publish {company-name}-curated or organization-curated reference `AiResource` entities, and provide a Software Template that helps teams create new entities with valid examples for both source repository and OCI image assets.
