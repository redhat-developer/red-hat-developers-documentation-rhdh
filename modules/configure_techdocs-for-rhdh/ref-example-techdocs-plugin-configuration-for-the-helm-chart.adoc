:_mod-docs-content-type: REFERENCE

[id="example-techdocs-plugin-configuration-for-the-helm-chart_{context}"]
= Example TechDocs Plugin configuration for the Helm chart

[role="_abstract"]
The following example shows a {product-short} Helm chart configuration for the TechDocs plugin.

[source,yaml,subs="+attributes,+quotes"]
----
global:
  dynamic:
    includes:
      - 'dynamic-plugins.default.yaml'
  plugins:
    - disabled: false
      package: ./dynamic-plugins/dist/backstage-plugin-techdocs-backend-dynamic
      pluginConfig:
        techdocs:
          builder: external
          generator:
            runIn: local
          publisher:
            awsS3:
              bucketName: '${BUCKET_NAME}'
              credentials:
                accessKeyId: '${aws-short}_ACCESS_KEY_ID}'
                secretAccessKey: '${aws-short}_SECRET_ACCESS_KEY}'
              endpoint: 'https://${BUCKET_HOST}'
              region: '${BUCKET_REGION}'
              s3ForcePathStyle: true
            type: awsS3
----
