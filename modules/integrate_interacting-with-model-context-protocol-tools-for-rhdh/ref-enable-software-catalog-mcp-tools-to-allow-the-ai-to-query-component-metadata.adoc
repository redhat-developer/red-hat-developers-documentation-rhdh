:_mod-docs-content-type: REFERENCE

[id="enable-software-catalog-mcp-tools-to-allow-the-ai-to-query-component-metadata_{context}"]
= Enable Software Catalog MCP tools to allow the AI to query component metadata

[role="_abstract"]
Enable and use the Software Catalog MCP tools so that the AI assistant can seamlessly query internal software component metadata, relations, and ownership information on your behalf.

Use Software Catalog MCP tools to manage and retrieve {product-very-short} entities such as *Components*, *Systems*, *Resources*, *APIs*, *Locations*, *Users*, and *Groups*. 

Software Catalog tool reference:

The `software-catalog-mcp-extras` plugin provides tools to interact with the software catalog. By default, these tools return data in a JSON array format.

The following table lists the available tools in the `software-catalog-mcp-tool` plugin:

[cols="20%,35%,25%,10%,10%",options="header"]
|===
|Tool |Description |Parameters |readOnly |destructive
|`fetch-catalog-entities` |Lists {product-very-short} entities such as components, APIs, and resources. |`kind`, `type`, `name`, `owner`, `lifecycle`, `tags`, `verbose` |`true` |`false`
|`catalog-register-tool` |Registers a new entity in the software catalog using a `catalog-info.yaml` file URL. |`url` |`false` |`false`
|`catalog-unregister-tool` |Removes an existing entity from the software catalog. |`entityRef` |`false` |`true`
|`software-template-metadata-tool` |Retrieves metadata for a specific software template. |`templateRef` |`true` |`false`
|===